# Backend/requirements.txt

#Utilities
pytz==2025.2 #- World timezone definitions and conversions
packaging==24.2 #- Core utilities for Python package version parsing and comparison
pyparsing==3.2.1 #- General parsing library for creating parsers
MarkupSafe==3.0.2 #- Safely escapes strings for use in HTML/XML (prevents injection attacks)
cachelib==0.13.0 #- Collection of cache storage backends (file, Redis, memcached, etc.)
cachetools==6.2.1 #- Provides decorators and data structures for caching/memoization
msgspec==0.19.0 #- Fast JSON/MessagePack/YAML serialization and validation library

#Network & API Libraries
requests==2.32.5 #- User-friendly HTTP library for making API calls
urllib3==2.5.0 #- HTTP client library (underlying transport for requests)
certifi==2025.8.3 #- Collection of root SSL certificates for verifying HTTPS connections
charset-normalizer==3.4.3 #- Detects character encoding in HTTP responses
idna==3.10 #- Handles Internationalized Domain Names in Applications (IDNA)

# Database Libraries
pymongo==4.15.1 #- MongoDB driver for Python
mongomock==4.3.0 #- In-memory MongoDB mock for testing
dnspython==2.8.0 #- DNS toolkit (required for MongoDB connection strings)

# Authentication Libraries
google-auth==2.41.1 #- Google authentication library
pyasn1==0.6.1 #- ASN.1 types and codecs (used by google-auth)
pyasn1_modules==0.4.2 #- ASN.1 protocol modules
rsa==4.9.1 #- RSA encryption implementation

dotenv==0.9.9  #- Loads environment variables from a .env file for configuration management
python-dateutil==2.9.0.post0 #- Provides extensions to the standard datetime module (used for date parsing/manipulation)

#Web Framework & Core API Libraries
Flask==3.1.2 #- Lightweight web framework for building the application
flask-cors==6.0.1 #- Handles Cross-Origin Resource Sharing (CORS) for API requests
Flask-Session==0.8.0 #- Server-side session management for Flask (listed twice in your file)
Jinja2==3.1.6 #- Template engine used by Flask for rendering HTML
Werkzeug==3.1.3 #- WSGI utility library that Flask is built on
gunicorn==23.0.0 #- Production WSGI server for deploying Flask apps
click==8.3.0 #- Command-line interface creation toolkit (used by Flask)
itsdangerous==2.2.0 #- Secure data signing (used for sessions/cookies)
blinker==1.9.0 #- Signal/event system used by Flask

sib-api-v3-sdk==7.6.0 #- Sendinblue (Brevo) API client for transactional emails

#Data Visualization & Scientific Libraries (Future Use?)
#matplotlib==3.10.0 - 2D plotting library
#numpy==2.2.2 - Numerical computing with arrays/matrices
#nnfs==0.5.1 - Neural Networks from Scratch support package
#contourpy==1.3.1 - Contour calculation (used by matplotlib)
#cycler==0.12.1 - Composable style cycles (used by matplotlib)
#fonttools==4.55.6 - Font manipulation library (matplotlib dependency)
#kiwisolver==1.4.8 - Constraint solver (used by matplotlib layout)
#pillow==11.1.0 - Image processing library
#networkx==3.5 - Graph/network data structures and algorithms

# Testing Libraries
pytest==8.4.2 #- Testing framework for writing and running tests
pluggy==1.6.0 #- Plugin management system used by pytest
iniconfig==2.1.0 #- Parser for INI configuration files (pytest dependency)
sentinels==1.1.1 #- Creates sentinel objects for use in tests (unique placeholder values)
